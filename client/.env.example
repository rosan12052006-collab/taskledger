# Leave blank for local dev (falls back to http://localhost:5001/api automatically).
# When deploying, set this to your deployed backend's URL + /api,
# e.g. VITE_API_URL=https://task-ledger-server.onrender.com/api
VITE_API_URL=
